/*
 * ResponseSettings Messages
 *
 * This contains all the text for the ResponseSettings component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  payloadError: {
    id: 'app.components.ResponseSettings.payloadError',
    defaultMessage: 'Please specify the payload value. It should be a JSON object.',
  },
});
